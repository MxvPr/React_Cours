
const DemoEvent = () => {

    const displayAlert = () => {
        alert('Vous avez cliqué eh eh')
    }

    const displayGoogle = (event) => {
        a

    }

  return (
        <>
        <button onClick={displayAlert}>Cliqué sur moi !!</button>
        <button onClick={() => alert('bien !!')}>Cliqué sur moi !!</button>
        <a onClick={displayGoogle}href="https://www.google.fr">Google</a>
        </>
  )
}

export default DemoEvent
